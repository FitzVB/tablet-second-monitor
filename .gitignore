# Rust
**/target/
Cargo.lock
**/*.rs.bk
*.pdb

# Android/Gradle
.gradle/
.idea/
*.iml
local.properties
build/
captures/
.externalNativeBuild/
.cxx/

# IDE
.vscode/
*.swp
*.swo
*~
.DS_Store

# Environment
.env
.env.local

# Distribution build artifacts
dist/
.cache/
*.zip

# Local debug captures (manual QA artifacts)
tm_*.png
tm_ui*.xml

# Host runtime settings — machine-specific (encoder learned per-device)
# Do not commit: each machine learns its own best encoder automatically.
host-settings.json
**/host-settings.json

# Local identity (do not commit)
identity.local.md
graphic_identity.md
CHAT_CONTEXT_HANDOFF.md
FlexDisplay_Logo.png
android-client/app/src/main/res/drawable/flexdisplay_logo.png

# Local packaged APK (manual/internal distribution)
FlexDisplay.apk
